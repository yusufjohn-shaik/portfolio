// No published blogs yet — placeholder structure ready for future posts
export const blogs = []

export const blogCategories = ['All', 'Frontend', 'Backend', 'DSA', 'AI']
export const getFeaturedBlogs = () => blogs.filter(b => b.featured)
